begin
  int i;
  int nota1, nota2, nota3;
  boolean aprovado = false;
  int soma;
  soma = nota1 + nota2 + nota3;
  if (soma >= "a"
  /* if (soma >= 7) */
     writeln,"Aprovado"
 else writeln, "Reprovado";
end.
